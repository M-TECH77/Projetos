## Para executar o app utilize:

`npm run dev`

## Para compilar o app .ts para .js utilize:

`tsc` para compilar toda pasta, ou, `tsc` e <nomeDoArquivo.ts> para compilar um único arquivo.

_SUCESSO!_
